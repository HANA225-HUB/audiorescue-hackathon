contract_version: "v0.1-contract"

# Only development fixtures are frozen today. Competition demo samples must be
# added here by C after consent, transcription review, listening review, and a
# full offline run. B must not hard-code paths outside this manifest.
samples:
  - id: "DEV_SMOKE_S01_FAN"
    name: "开发联调：合成啁啾与风扇噪声"
    file: "tests/fixtures/dev_smoke_s01_fan.wav"
    reference_text: null
    intended_use: "development_only"
    default: true
    enable_events: false
    sha256: "ad849a4924f4fd4e8bb53f590bf70d99c99dae498836a569d1bff38e14ddef04"
