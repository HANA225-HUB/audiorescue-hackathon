# AudioRescue / MeetingEverywhere 比赛封存说明

封存日期：2026-07-16
状态：比赛结束，仓库进入只读纪念与复现实验状态。

## 最终作品

本项目将两条能力合并为一个面向复杂环境会议的原型：

1. **实时声音恢复**：在 Apple Silicon Mac 上使用 GTCRN / sherpa-onnx 做麦克风实时降噪，并通过 BlackHole 虚拟麦克风发送给会议软件；安静环境下提供保真人声的小声增强模式。
2. **会议上下文助手**：会前录入场景、身份、目标和议程，并上传 PDF、PPTX、DOCX、TXT 或 Markdown 参考资料；会议中使用实时转写、局部检索、议程状态和结构化生成，为悬浮窗提供下一段建议与提问回答。
3. **离线音频急救链路**：保留标准化、增强、双路转写、文本差异、可视化和结果持久化能力，用于文件后处理与对比验证。

最终产品入口为 `app.py`，实时音频命令行入口为 `scripts/live_gtcrn.py`。完整运行方式见 [README.md](README.md)。

## 封存内容

- 最终源码、配置、轻量测试与公开合成 fixture；
- 实时降噪、小声增强、实时 ASR、会议上下文和悬浮窗实现；
- 团队协作文档、运行手册和测试说明；
- Transformer 小型答辩的公开 Demo 预设、口播稿、资料与录制方案，见 `demo_assets/transformer_defense_demo/`。

## 未进入公开仓库的内容

为保护隐私并控制仓库体积，下列内容没有上传：

- 用户或队员的真实录音、原始 Demo 视频和含个人音频的演示文稿；
- API Key、环境变量、终端历史和本机绝对路径；
- Conda/venv 环境、模型权重、模型缓存和运行输出；
- 未授权数据集、逐样例评测台账和临时文件。

## 已知边界

- 会议软件对虚拟麦克风的识别和自带降噪策略不同，正式使用前需要逐软件验证；
- 当前远端参会者的问题主要通过悬浮窗输入，未实现稳定的系统回环音频采集；
- 实时转写与模型建议依赖外部服务和网络，不应与本地实时降噪的稳定性混为一谈；
- 这是黑客松原型，不是面向生产环境的音频驱动或会议合规产品。

## 复现原则

优先使用 Python 3.10/3.11，并按 `requirements.txt` 安装项目依赖。真实模式使用：

```bash
AUDIORESCUE_UI_FIXTURE=0 python app.py
```

只验证页面和公开 fixture 时使用：

```bash
AUDIORESCUE_UI_FIXTURE=1 python app.py
```

公开封存只保证源码与轻量测试可追溯；真实设备、模型下载、云端 API 和会议软件兼容性仍需在目标机器上单独配置。
